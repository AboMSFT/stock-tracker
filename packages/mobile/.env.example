# Base URL for the Azure Functions API.
# Defaults to the live production deployment if not set.
# Override for local dev with the Vite dev server URL or a local Azure Functions emulator.
EXPO_PUBLIC_API_BASE_URL=https://ambitious-sand-0cdb1aa10.7.azurestaticapps.net
